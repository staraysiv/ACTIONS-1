name: My First Workflow

on: push 

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
            - name: Chekout Repo
              uses: actions/checkout@v4
            - name: Welcome Message
              run: echo "My First Workflow Job"

            - name: List Files
              run: ls -ltr

            - name: Read file
              run: cat README.md

